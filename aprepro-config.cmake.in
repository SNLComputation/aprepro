# Compute paths
get_filename_component(aprepro_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET aprepro AND NOT aprepro_BINARY_DIR)
  include("${aprepro_CMAKE_DIR}/aprepro-targets.cmake")
endif()
